# React Native Todo List with Cypress and Lambda
This sample project is to test Cypress in React Native App. And at the same time try lambda with its parallelization in a CI/CD build using localhost.

# Installation
```
npm run install
```
